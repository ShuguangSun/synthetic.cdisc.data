

# add helper functions


# create the recipe

_rel_join_recipe
adeg_reicpe <- tribble(
  ~variables,       ~dependencies,   ~func,                  ~func_args,
  ...
)

# create the data
gen_reljoin_table(...)
